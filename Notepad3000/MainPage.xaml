<Page
    x:Class="Notepad3000.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Notepad3000"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

       <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Height="Auto" Grid.Row="0">
            <Button x:Name="FileButton" Content="File" Click="MenuButtonClicked">
                <Button.Flyout>
                    <MenuFlyout Placement="Bottom">
                        <MenuFlyoutItem Text="New" Click="NewClicked" />
                        <MenuFlyoutItem Text="Open..." />
                        <MenuFlyoutItem Text="Save" />
                        <MenuFlyoutItem Text="Save As..." />
                        <MenuFlyoutSeparator/>
                        <MenuFlyoutItem Text="Exit" />
                    </MenuFlyout>
                </Button.Flyout>
            </Button>
            <Button x:Name="EditButton" Content="Edit" Click="MenuButtonClicked">
                <Button.Flyout>
                    <MenuFlyout Placement="Bottom">
                        <MenuFlyoutItem Text="Undo" />
                        <MenuFlyoutSeparator/>
                        <MenuFlyoutItem Text="Cut" />
                        <MenuFlyoutItem Text="Copy" />
                        <MenuFlyoutItem Text="Paste" />
                        <MenuFlyoutSeparator/>
                        <MenuFlyoutItem Text="Select All" />
                    </MenuFlyout>
                </Button.Flyout>
            </Button>
            <Button x:Name="FormatButton" Content="Format" Click="MenuButtonClicked">
           
                <Button.Flyout>
                    <MenuFlyout Placement="Bottom">
                       
                        <MenuFlyout.MenuFlyoutPresenterStyle>
                            <Style TargetType="MenuFlyoutPresenter">
                               
                            </Style>
                        </MenuFlyout.MenuFlyoutPresenterStyle>
                        <MenuFlyoutItem Text="Font" />
                    </MenuFlyout>
                </Button.Flyout>
            
                
            </Button>
            <Button x:Name="HelpButton" Content="Help" Click="MenuButtonClicked">
             
                <Button.Flyout>
                    <MenuFlyout Placement="Bottom">
                        <MenuFlyoutItem Text="About" />
                    </MenuFlyout>
                </Button.Flyout>
         
            </Button>
        </StackPanel>
       
       <ScrollViewer VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Hidden" Grid.Row="1">
               <RichEditBox x:Name="MainTextBox" FontFamily="Consolas" FontSize="14" IsSpellCheckEnabled="False" BorderThickness="1" KeyDown="TabKeyDown" />  

        </ScrollViewer>


    </Grid>
</Page>
